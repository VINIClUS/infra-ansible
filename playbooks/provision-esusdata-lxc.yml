---
- name: Provision the create-only esusdata LXC
  hosts: esusdata_bootstrap
  connection: local
  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"
  gather_facts: false
  serial: 1
  roles:
    - role: proxmox_lxc_guest
      tags: [proxmox_lxc_guest]
    - role: deploy_runner_ssh_access
      tags: [deploy_runner_ssh_access]
