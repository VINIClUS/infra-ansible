---
- name: Reconcile the Ansible controller edge route
  hosts: edge_proxy_hosts
  gather_facts: false
  serial: 1
  roles:
    - role: edge_proxy_route
      tags: [edge_proxy_route]
    - role: protocolos_edge_route
      tags: [protocolos_edge_route]
