---
all:
  children:
    local_validation:
      hosts:
        localhost:
          ansible_connection: local
    proxmox_nodes:
      hosts: {}
    linux_guests:
      hosts: {}
    esus_pec:
      hosts: {}
    sus_siha:
      hosts: {}
